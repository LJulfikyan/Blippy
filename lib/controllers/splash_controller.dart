import 'package:blippy_login/Base/app_config.dart';
import 'package:blippy_login/Base/base_controller.dart';
import 'package:blippy_login/Base/blippy_utils.dart';

class SplashController extends BaseController {
  SplashController()  {
    //  BlippyUtils.getCode();
    // print(AppConfig.countryCodeData?.countryCallingCode);
  }
  animateCompleted()  {
    if(AppConfig.countryCodeData != null) {

    }
    }
  }
